import type { ResizeConstraints } from './resizeConstraints.ts'

export const RICH_TEXT_CONSTRAINTS: ResizeConstraints = {
  minWidth: 250,
  maxWidth: null,
  minHeight: 160,
  maxHeight: null,
}

export const CODE_BLOCK_CONSTRAINTS: ResizeConstraints = {
  minWidth: 330,
  maxWidth: null,
  minHeight: 210,
  maxHeight: null,
}
